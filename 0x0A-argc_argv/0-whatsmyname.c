#include <stdio.h>

/**
 * main - Print program name
 * Return: 0 when done, 1 if error
 * --- by NPCdev ---
*/

int main(int argc, char **argv)
{
	(void) argc;
	printf("%s\n", argv[0]); 
	return (0);
}
